// Напишите программу, которая найдёт точку пересечения двух прямых,
//  заданных уравнениями y = k1 * x + b1, y = k2 * x + b2; 
//  значения b1, k1, b2 и k2 задаются пользователем.
// b1 = 2, k1 = 5, b2 = 4, k2 = 9 -> (-0,5; -0,5)

 Console.WriteLine("Введите значения b1, k1, b2 и k2:");

        // Ввод значений b1, k1, b2 и k2 с клавиатуры
        double b1 = double.Parse(Console.ReadLine());
        double k1 = double.Parse(Console.ReadLine());
        double b2 = double.Parse(Console.ReadLine());
        double k2 = double.Parse(Console.ReadLine());

        // Находим координаты точки пересечения
        double x = (b2 - b1) / (k1 - k2);
        double y = k1 * x + b1;

        // Выводим координаты точки пересечения
        Console.WriteLine($"Точка пересечения: ({x}; {y})");
    



// При вводе значений b1 = 2, k1 = 5, b2 = 4, k2 = 9 программа выведет 
//следующий результат:

// Введите значения b1, k1, b2 и k2:
// 2
// 5
// 4
// 9
// Точка пересечения: (-0,5; -0,5)